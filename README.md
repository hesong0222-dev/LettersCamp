# 레터스 굿즈 재고

부스에서 폰·태블릿으로 쓰는 재고 관리 페이지. `index.html` 하나로 끝나고 서버가 필요 없다 — GitHub Pages로 정적 호스팅된다.

- 종류마다 판매 버튼 하나, 사이즈가 있는 품목은 사이즈별 버튼
- 판매 확인 모달을 거쳐야 재고가 깎이고, 판매 기록에 남는다
- 판매 기록 줄별 취소, 가격 조정, 백업 저장/불러오기(JSON 파일)
- 화면 크기·방향에 맞춰 스크롤 없이 한 화면에 꽉 차게 배치된다
- 재고·가격·기록은 이 브라우저에 저장된다(기기별로 따로 관리됨)
